#pragma once
//struct Date specification

#include <iostream>
#include <string>
using namespace std;

//struct - a data type in which each object is a collection of components
struct Date
{
	//struct variables are called objects (data members)
	//string name;
	int month;
	int day;
	int year;
};